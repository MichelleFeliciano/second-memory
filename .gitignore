.DS_Store
Thumbs.db

# Device sync server — machine-specific secrets/state, never commit these.
.sync_secret
cert.pem
key.pem
sync_data.json

# Dev-process files — not part of the public repo.
.claude/
CLAUDE.md
DECISIONS.md
docs/
